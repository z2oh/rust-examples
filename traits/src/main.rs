mod point;

use point::Point;
//use point::{EuclideanDistance, Point};

fn main() {
    let origin = Point { x: 0.0, y: 0.0 };
    let one = Point { x: 1.0, y: 1.0 };
    let p1 = Point { x: 3.0, y: 4.0 };
    let p2 = Point { x: -6.0, y: 1.0 };

    /*
    let sum = p1 + p2;
    */

    /*
    println!("{}", sum);
    */
    
    /*
    for dim in sum {
        println!("{}", dim);
    }
    */

    /*
    let dist = origin.dist(&one);
    println!("{}", dist);
    */

}
